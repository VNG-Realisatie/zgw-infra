apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{ .Values.secret.key }}: {{ .Values.global.secret_keys.klanten | b64enc }}
  {{ .Values.secret.sentry }}: {{ .Values.global.sentry_dsn.klanten | b64enc }}
