apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.api_secrets.nrc.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  {{ .Values.api_secrets.key }}: {{ .Values.secret_keys.nrc | b64enc }}
