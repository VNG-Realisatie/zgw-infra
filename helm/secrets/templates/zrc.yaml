apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.api_secrets.zrc.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  {{ .Values.api_secrets.key }}: {{ .Values.secret_keys.zrc | b64enc }}
